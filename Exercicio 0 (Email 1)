#include <stdio.h>
 
int son(int x)
{
    if (x > 0)
    {
    	printf("Numero positivo\n");
	}
	else
	{
		printf("Numero negativo\n");
	}
}
 
int main()
{
	int numero;
    printf("Digite um numero inteiro: ");
    scanf("%d", &numero);
    son(numero);
}
